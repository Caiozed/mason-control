<Page x:Class="MasonControl.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MasonControl.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginView">

    <Grid>
        <Border Background="#19FFFFFF" HorizontalAlignment="Center" Padding="15" VerticalAlignment="Center" CornerRadius="5" BorderBrush="Gray" BorderThickness="2">
            <StackPanel Width="400" >
                <Label Content="Usuario" FontSize="20"/>
                <TextBox Foreground="White"  Text="{Binding Usuario}"/>

                <Label Content="Senha"  FontSize="20"/>
                <TextBox Foreground="White"  Text="{Binding Senha}"/>

                <Button Content="ENTRAR" Margin="20" Command="{Binding LoginCommand}"/>
            </StackPanel>
        </Border>
       
    </Grid>
</Page>
